{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zig build-exe",
            "type": "shell",
            "command": "zig build-exe main.zig --name debug_binary",
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}